# Aha 召回优化计划

状态：候选补位、trace、Session Store 验证及 Relation Judge 用户可见措辞 v8 已实施；query 优化、Judge 语义校准和批量实验尚未执行。

## 目标与基线

以**每篇 surprise 数量**为主指标，提高实际有价值的召回，不靠放宽关系判定减少 weak。

八月 10 篇：18 个 surprise，平均 **1.80 个/篇**；145 条候选中 57 条 weak。辅助指标为排除 weak 后的 surprise 率：合并 20.5%，逐篇平均 19.8%。

## 实施顺序

1. **先做候选补位**：保留现有 query 和 Judge prompt；按检索顺序分批判断，weak 不占非 weak 候选名额，继续从候选池补入。达到目标数量、候选池耗尽或判断预算上限即停止。
2. **完善 trace**：记录每批判断、补位来源、停止原因、调用次数与耗时；展示结果和 trace 引用写入 Session Store，已有反馈不清空。
3. **验证后再改 query**：针对高 weak 案例，减少情绪同义扩写，增加机制与真正的反例查询。与补位改动分开评估。
4. **单独校准 Judge**：先明确 source 的核心判断，再判支持/挑战方向；抽查 weak 的误判及长笔记摘录遗漏，不直接降低引句要求。

## 验证方式

- 在相同 source、固定查询结果和候选池上比较补位前后，先隔离检索变化。
- 记录每篇新增 surprise 数、非 weak 数、补位轮数及额外耗时；surprise 率只作辅助。
- 新增候选由用户标注；未标注不算“没有 surprise”，旧 surprise 也不自动代表新实验成功。
- 检查自命中、重复候选、预算停止、失败恢复，以及重载后反馈和 trace 引用是否保留。

## 边界

- 每轮只改变一个环节；先确定非 weak 目标数量和判断预算，再执行实验。
- 原笔记、现有标注和基线 trace 不覆盖。
- 实验笔记清单与批量执行仍需用户确认。

## 已实施参数与行为（2026-09-01）

- `targetCandidates` 现在表示每轮希望得到的非 weak 候选数，界面范围仍为 15–20。
- 新增 `relationJudgeBudget`，默认 40、界面范围 20–60；它限制每轮最多判断的候选摘录数。
- 首批按候选池顺序取当前非 weak 缺口数量；weak 或判断失败不会满足目标，后续批次继续顺序补位。
- 停止原因固定记录为 `target_reached`、`pool_exhausted` 或 `budget_exhausted`。
- Full Tier trace 的 `steps.rerank.backfill` 记录每批候选范围、补位来源、非 weak/weak/失败/修复数量、API 调用次数及耗时。
- 展示候选与 trace 引用继续写入 Session Store；重载和后续重跑不会清空既有用户反馈。
- 候选补位提交本身没有改 query 或 Judge prompt；随后仅单独迭代用户可见措辞 v8。尚未执行任何批量实验，实验笔记清单仍需用户确认。

## Relation Judge 措辞 v8（2026-09-01）

- `why` 直接陈述具体经验、判断、矛盾或边界，不再复述“旧笔记 / 候选 / excerpt / source / 当前 insight”等管线术语。
- 英文词只有在当前材料或历史材料中已经出现时才允许进入 `why`，避免无来源的中英混杂。
- 第一次输出若仍含管线术语或无来源英文，会触发一次文风重写；这不改变 relation 标签和引句证据门槛。
- 已用真实 DeepSeek 对单条和三条不同材料进行验证：输出分别形成支持、挑战和边界表达，措辞与开头不相同。
- 本轮只优化用户可见措辞与真实输出兼容性；未改 query，也未声称完成 weak 语义误判校准或正式批量实验。
